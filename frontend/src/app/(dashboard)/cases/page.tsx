import { FeaturePlaceholder } from "@/components/ui/feature-placeholder";

export default function CasesPage() {
  return <FeaturePlaceholder title="Cases" description="Coordinate ownership, human review, case actions, and auditable resolution." />;
}
