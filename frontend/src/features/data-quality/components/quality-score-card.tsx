export function QualityScoreCard({ label, value }: { label: string; value: number }) { const percent=Math.round(value*100); return <div><div className="flex justify-between text-sm"><span>{label}</span><strong>{percent}%</strong></div><div role="progressbar" aria-label={`${label}: ${percent}%`} aria-valuemin={0} aria-valuemax={100} aria-valuenow={percent} className="mt-2 h-2 rounded-full bg-[var(--color-border)]"><div className="h-full rounded-full bg-[var(--color-accent)]" style={{width:`${percent}%`}} /></div></div>; }
