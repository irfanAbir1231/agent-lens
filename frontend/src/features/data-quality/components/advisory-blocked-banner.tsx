export function AdvisoryBlockedBanner({blocked}:{blocked:boolean}) { return <div role="status" className={`rounded-lg border-2 p-5 ${blocked?"border-[var(--color-warning)] bg-[var(--color-warning-soft)]":"border-[var(--color-healthy)] bg-[var(--color-healthy-soft)]"}`}><h2 className="font-bold">{blocked?"Rocket AI advisory blocked":"Rocket feed recovered"}</h2><p className="mt-2 text-sm leading-6">{blocked?"The Rocket feed is 22 minutes old. Forecast confidence was reduced and AI-generated recommendations are blocked until the feed is refreshed or manually verified.":"Rocket data is healthy, forecast confidence is restored, and AI advisory is allowed."}</p></div>; }
