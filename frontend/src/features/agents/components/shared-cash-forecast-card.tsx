import { ConfidenceBar } from "@/components/ui/confidence-bar";
import { StatusBadge } from "@/components/ui/status-badge";
import type { SharedCashForecastViewModel } from "../agent-detail-view-model";

export function SharedCashForecastCard({ forecast }: { forecast: SharedCashForecastViewModel }) {
  return <section className="rounded-lg border border-[var(--color-border)] bg-[var(--color-panel)] p-5 shadow-panel"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-lg font-bold text-[var(--color-text-primary)]">Shared physical cash forecast</h2><p className="mt-1 text-sm text-[var(--color-text-secondary)]">Agent-scoped reserve; provider balances are not combined.</p></div><StatusBadge label={forecast.statusLabel} tone={forecast.statusTone} /></div><div className="mt-5 grid gap-4 md:grid-cols-3"><div><p className="text-sm text-[var(--color-text-secondary)]">Current shared cash</p><p className="mt-1 text-xl font-bold">{forecast.balance}</p></div><div><p className="text-sm text-[var(--color-text-secondary)]">Estimated shortage</p><p className="mt-1 font-semibold">{forecast.shortageLabel}</p></div><div><p className="text-sm text-[var(--color-text-secondary)]">Prediction source</p><p className="mt-1 font-semibold">{forecast.predictionSourceLabel}</p></div></div><div className="mt-5"><ConfidenceBar label="Shared-cash forecast confidence" value={forecast.confidence} description={forecast.confidenceLabel} /></div><p className="mt-4 rounded-md bg-[var(--color-panel-subtle)] p-3 text-sm text-[var(--color-text-secondary)]">{forecast.limitation}</p></section>;
}
