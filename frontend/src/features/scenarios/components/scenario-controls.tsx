export interface ScenarioControlsValue {provider:string;demand:number;cash:number;delay:number;repeated:boolean;nearby:boolean;eid:boolean}
export function ScenarioControls({value,onChange}:{value:ScenarioControlsValue;onChange:(value:ScenarioControlsValue)=>void}) { const set=<K extends keyof ScenarioControlsValue>(key:K,next:ScenarioControlsValue[K])=>onChange({...value,[key]:next}); return <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><label>Provider<select value={value.provider} onChange={e=>set("provider",e.target.value)} className="mt-1 min-h-10 w-full rounded-md border px-3"><option>NAGAD</option><option>BKASH</option><option>ROCKET</option></select></label><label>Demand multiplier: <strong>{value.demand.toFixed(1)}x</strong><input type="range" min="1" max="4" step="0.1" value={value.demand} onChange={e=>set("demand",Number(e.target.value))} className="w-full"/></label><label>Shared physical cash: <strong>BDT {value.cash}</strong><input type="range" min="10000" max="200000" step="5000" value={value.cash} onChange={e=>set("cash",Number(e.target.value))} className="w-full"/></label><label>Feed delay minutes<input type="number" min="0" value={value.delay} onChange={e=>set("delay",Number(e.target.value))} className="mt-1 min-h-10 w-full rounded-md border px-3"/></label>{[["repeated","Repeated transactions"],["nearby","Nearby outlet available"],["eid","Eid context"]].map(([key,label])=><label key={key} className="flex min-h-10 items-center gap-2"><input type="checkbox" checked={Boolean(value[key as keyof ScenarioControlsValue])} onChange={e=>set(key as "repeated"|"nearby"|"eid",e.target.checked)}/>{label}</label>)}</div>; }
