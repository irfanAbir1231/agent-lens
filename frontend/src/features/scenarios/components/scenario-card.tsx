import { Button } from "@/components/ui/button"; import type { ScenarioPreset } from "@/lib/api/scenarios";
export function ScenarioCard({preset,onRun}:{preset:ScenarioPreset;onRun:(preset:ScenarioPreset)=>void}) { return <article className="rounded-lg border border-[var(--color-border)] bg-[var(--color-panel)] p-5"><h3 className="font-bold">{preset.name}</h3><p className="mt-2 text-sm text-[var(--color-text-secondary)]">{preset.description}</p><p className="mt-3 text-xs font-semibold">Injected conditions</p><ul className="my-2 list-inside list-disc text-sm">{preset.conditions.map(x=><li key={x}>{x}</li>)}</ul><p className="mb-4 text-sm"><strong>Expected:</strong> {preset.response}</p><Button onClick={()=>onRun(preset)}>Run {preset.name}</Button></article>; }
