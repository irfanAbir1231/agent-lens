import { Fragment } from "react"; import { Button } from "@/components/ui/button"; import { DataTable } from "@/components/ui/data-table"; import type { AuditRow } from "@/lib/api/audit"; import { AuditEventDetail } from "./audit-event-detail";
export function AuditTable({rows,selected,onSelect}:{rows:AuditRow[];selected:string;onSelect:(id:string)=>void}){return <DataTable caption="Audit events"><thead><tr>{["Time","Actor","Role","Action","Provider","Resource","Previous state","New state","Reason","Detail"].map(h=><th key={h} scope="col" className="px-3 py-3 text-xs uppercase">{h}</th>)}</tr></thead><tbody>{rows.map(r=><Fragment key={r.id}><tr className="border-t"><td className="whitespace-nowrap px-3 py-4">{r.time}</td><td className="px-3">{r.actor}</td><td className="px-3">{r.role}</td><td className="px-3">{r.action}</td><td className="px-3">{r.provider}</td><td className="px-3">{r.resource}</td><td className="px-3">{r.previous}</td><td className="px-3">{r.next}</td><td className="px-3">{r.reason}</td><td className="px-3"><Button variant="outline" onClick={()=>onSelect(selected===r.id?"":r.id)}>{selected===r.id?"Hide details":"View details"}</Button></td></tr>{selected===r.id?<tr><td colSpan={10} className="p-3"><AuditEventDetail row={r}/></td></tr>:null}</Fragment>)}</tbody></DataTable>}
