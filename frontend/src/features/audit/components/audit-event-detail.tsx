import type { AuditRow } from "@/lib/api/audit";
export function AuditEventDetail({row}:{row:AuditRow}){return <dl className="grid gap-2 rounded-md bg-[var(--color-panel-subtle)] p-4 sm:grid-cols-2">{[["Event ID",row.id],["Timestamp",row.timestamp],["Actor",row.actor],["Role",row.role],["Action",row.action],["Provider",row.provider],["Resource",row.resource],["Previous state",row.previous],["New state",row.next],["Reason",row.reason],["Indicator",row.synthetic?"Synthetic demo event":"Live event"]].map(([k,v])=><div key={k}><dt className="text-xs font-semibold uppercase">{k}</dt><dd>{v}</dd></div>)}</dl>}
