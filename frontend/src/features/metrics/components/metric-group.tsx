import { MetricCard } from "@/components/ui/metric-card"; import { Panel } from "@/components/ui/panel"; import type { ReactNode } from "react"; import type { MetricGroupViewModel } from "../metrics-view-model";
export function MetricGroup({title,description,group,children}:{title:string;description:string;group:MetricGroupViewModel;children?:ReactNode}) { return <Panel title={title} description={description}><p className={`mb-5 rounded-md border p-3 text-sm font-semibold ${group.availability === "AVAILABLE" ? "border-[var(--color-healthy)] bg-[var(--color-healthy-soft)]" : "border-[var(--color-warning)] bg-[var(--color-warning-soft)]"}`}>{group.metadataLabel}</p><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{group.metrics.map(m=><MetricCard key={m.label} {...m}/>)}</div>{children}</Panel>; }
